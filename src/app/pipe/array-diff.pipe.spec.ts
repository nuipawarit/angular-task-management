import { ArrayDiffPipe } from './array-diff.pipe';

describe('ArrayDiffPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayDiffPipe();
    expect(pipe).toBeTruthy();
  });
});
