import { DateTimeThaiPipe } from './date-time-thai.pipe';

describe('DateTimeThaiPipe', () => {
  it('create an instance', () => {
    const pipe = new DateTimeThaiPipe();
    expect(pipe).toBeTruthy();
  });
});
